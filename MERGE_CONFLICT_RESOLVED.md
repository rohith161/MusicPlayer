# PR #1 conflict resolution

The PR branch reconciles the newer `main` player UI and behavior with the PR's MediaRouter output switching. This marker documents that the conflict was resolved on the feature branch before merging into `main`.
